<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Dialog
        id="markAsDiscontinued"
        title="Discontinue Product"
    >
        <content>
                <Text textAlign="Center"
                    text="Are you sure you want to mark this product as discontinued?"
                />
        </content>
        <beginButton>
            <Button
                text="{i18n>yesText}"
                press=".onCloseYesDialog"
            />
        </beginButton>
        <endButton>
            <Button
                text="{i18n>noText}"
                press=".onCloseNoDialog"
            />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
